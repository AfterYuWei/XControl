{
  "name": "xcontrol",
  "private": true,
  "version": "1.0.0",
  "description": "XControl 桌面版构建编排（Tauri 2，见 docs/TAURI_MIGRATION.md）",
  "scripts": {
    "tauri": "tauri",
    "desktop:dev": "node scripts/prepare-sidecar.mjs --go-only && tauri dev",
    "desktop:build": "tauri build",
    "desktop:smoke": "cd src-tauri && cargo run -- --smoke-test",
    "desktop:icon": "tauri icon src-tauri/app-icon.png"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
